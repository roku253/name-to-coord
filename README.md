# 氏名 → 座標（サービス終了）

v3『ここにいる』では本ツールは**廃止**済みです。

- 公開ページは「サービス終了」の静的 `index.html` のみ
- 旧パズル用の変換ロジック・token-gate は撤去済み
- ポータルの `external-investigation-auth` からも `ext:name-to-coord` を外済み

誤って再公開・再配線しないでください。
